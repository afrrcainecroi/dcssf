#pragma once

#include <iostream>
#include <sstream>
#include <iterator>
#include <list>
#include <string>

using namespace std;

int mtfa_fuzzy_find(string pattern, string toSearch, int maxops);
